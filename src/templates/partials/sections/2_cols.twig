{% set section = data %}

<section class="cols">
    <div class="container">
        {% if section.title %}
            <h2 class="advantages__title">{{ section.title }}</h2>
        {% endif %}
    </div>
</section>
