.faq {
    padding: 60px 0;

    &__title {
        @mixin font-headline-xl;

        margin: 0;
        text-align: center;
    }

    &__content {
        margin: 60px auto 0;
        max-width: 814px;
    }
}
